From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Rendy Sebpian <nightcoremosta@gmail.com>
Date: Sun, 26 Jul 2026 23:15:40 +0700
Subject: [PATCH 4/4] Document SceneFX window effects


diff --git a/docs/labwc-actions.5.scd b/docs/labwc-actions.5.scd
index 2677e34..4d7ded0 100644
--- a/docs/labwc-actions.5.scd
+++ b/docs/labwc-actions.5.scd
@@ -202,6 +202,10 @@ argument. Default is "center".
 </keybind>
 ```
 
+*<action name="ToggleWindowEffects" />*
+	Toggle the configured opacity and blur effects for the focused window.
+	Fullscreen windows remain opaque with blur disabled.
+
 *<action name="SetDecorations" decorations="value" forceSSD="no" />*
 	Set decorations of focused window.
 
diff --git a/docs/labwc-config.5.scd b/docs/labwc-config.5.scd
index 6a16b89..97d4e1b 100644
--- a/docs/labwc-config.5.scd
+++ b/docs/labwc-config.5.scd
@@ -679,6 +679,52 @@ extending outward from the snapped edge.
 	Font weight (normal, thin, ultralight, light, semilight, book, medium,
 	semibold, bold, ultrabold, heavy, ultraheavy). Default is normal.
 
+## WINDOW EFFECTS
+
+Window effects apply to the content of regular Wayland and XWayland windows.
+Popups, menus, server-side decorations and captured window content are
+unaffected. Fullscreen windows are always opaque and have blur disabled to
+preserve direct scanout.
+
+Layer-shell effects apply only to surfaces that request a non-empty blur
+region using ext-background-effect-v1. This lets clients select which shell
+surfaces participate in compositor opacity and blur.
+
+*<windowEffects><enabled>* [yes|no]
+	Enable window opacity and blur. Default is no.
+
+*<windowEffects><activeOpacity>*
+	Opacity of the active window, between 0.0 and 1.0. Default is 1.0.
+
+*<windowEffects><inactiveOpacity>*
+	Opacity of inactive windows, between 0.0 and 1.0. Default is 1.0.
+
+*<windowEffects><layerShell><enabled>* [yes|no]
+	Enable opacity and blur for participating layer-shell surfaces. Default
+	is no.
+
+*<windowEffects><layerShell><opacity>*
+	Opacity of participating layer-shell surfaces, between 0.0 and 1.0.
+	Default is 1.0.
+
+*<windowEffects><blur><enabled>* [yes|no]
+	Blur the scene behind translucent windows. Default is no.
+
+*<windowEffects><blur><passes>*
+	Number of blur passes, between 0 and 10. Default is 3.
+
+*<windowEffects><blur><radius>*
+	Blur radius, between 0 and 10. Default is 5.
+
+*<windowEffects><blur><noise>*
+	Noise applied to the blur, between 0.0 and 1.0. Default is 0.02.
+
+*<windowEffects><blur><brightness>*++
+*<windowEffects><blur><contrast>*++
+*<windowEffects><blur><saturation>*
+	Blur color adjustments, each between 0.0 and 2.0. Defaults are 0.9,
+	0.9 and 1.1, respectively.
+
 ## MARGIN
 
 *<margin top="" bottom="" left="" right="" output="" />*
diff --git a/docs/rc.xml.all b/docs/rc.xml.all
index 350d2ea..de4dd66 100644
--- a/docs/rc.xml.all
+++ b/docs/rc.xml.all
@@ -78,6 +78,25 @@
     </font>
   </theme>
 
+  <windowEffects>
+    <enabled>no</enabled>
+    <activeOpacity>1.0</activeOpacity>
+    <inactiveOpacity>1.0</inactiveOpacity>
+    <layerShell>
+      <enabled>no</enabled>
+      <opacity>1.0</opacity>
+    </layerShell>
+    <blur>
+      <enabled>no</enabled>
+      <passes>3</passes>
+      <radius>5</radius>
+      <noise>0.02</noise>
+      <brightness>0.9</brightness>
+      <contrast>0.9</contrast>
+      <saturation>1.1</saturation>
+    </blur>
+  </windowEffects>
+
   <windowSwitcher preview="yes" outlines="yes" unshade="yes">
     <osd show="yes" style="classic" output="all" thumbnailLabelFormat="%T" />
     <fields>
